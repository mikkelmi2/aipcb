# M12 — signal-integrity simulation: delivery report

`aipcb simulate` exists. It cuts one slice per differential pair out of a routed
board, hands each slice to openEMS through Antmicro's gerber2ems in the container
[ADR 0011](../decisions/0011-si-simulation.md) pinned, and turns the S-parameters
that come back into the same source-referenced findings as every other check.

Everything below was measured on 2026-08-21 on the development machine (16 cores,
30 GB RAM), on the tree as committed. Where a number is an estimate or a closed form
rather than a measurement, it says so.

## Deviations, stated loudly

| Item | What happened |
|---|---|
| *"one pair simulated on `examples/mcu-4layer`, results within a plausible sanity band of its design target"* | **Simulated and converged, but the band had to be defined against physics the design target does not model.** The USB pair reads **≈74 Ω** differential. Both closed forms aipcb carries say 121 Ω (IPC-2141) and 127 Ω (Hammerstad) — and both are *bare microstrip formulas applied to a trace with a ground pour 0.2 mm away on each side*. The pair is a coplanar waveguide with ground, not a microstrip, and a 40 % drop is what that does. The band chosen, and the argument for it, are below under "The calibration". |
| *"`examples/diff-pair`'s declared 100 Ω is unreachable"* | **Confirmed, with different numbers than phase 0 assumed.** ADR 0011 wrote ≈270 Ω from a loosely-coupled reading of Hammerstad. aipcb's own formulas, using its own coupling factor, give **151 Ω (IPC-2141) / 156 Ω (Hammerstad)** for that geometry. Either way 100 Ω is not reachable and the example's target is wrong; the correction is recorded here rather than left as a discrepancy between two documents. |
| *"`pcie-sata`: all routed pairs simulated; the structured report renders with per-pair metrics; at least the impedance-vs-target comparison works against the M11a-declared targets"* | **Met.** All eleven logical links on `examples/pcie-sata` were simulated and every one carries a per-pair impedance-against-target row under “The batch”, with return loss, insertion loss, mode conversion, group delay and a verdict beside it. |
| *"whether simulation discriminates the two pairs M11 delivered over their skew budget"* | **Measured, and the answer is no.** Worst in-band \|Scd21\| is **−23.0 dB** for `REFCLKP/N` (0.292 mm of skew) and **−25.2 dB** for `PCIE_RXP/N` (0.256 mm) — and both **pass** the −20 dB default. The three links that come back `mode_conversion=warn` are `SATA3_RXP/N` (−14.3 dB), `SATA3_TXP/N` (−15.7 dB) and `SATA2_TXP/N` (−19.4 dB), whose skews are **0.004, 0.042 and 0.053 mm** against a 0.25 mm budget. The verdict is therefore not merely uninformative about skew on this board: it flags three well-matched pairs and passes both mismatched ones. The cause is a mode-conversion floor that varies by more than 25 dB from pair to pair, while the skew under test moves the number by about 3 dB. Worked through under “The two pairs M11 left open”. |
| Eight pairs of one class, alike in every declared way, simulate **29 Ω apart** | **Measured, unexplained, and the tightest bound in this report on what a single number is worth.** The eight `sata` links share a class, a derived width and gap, a layer, and coupled lengths inside 30.9–31.4 mm, with M11 measuring width and gap deviation at 0.0 on every one. They simulate at 62.5 to 91.9 Ω — −37.5 % to −8.1 % against one 100 Ω target. Deviation, ripple width and return loss move together across the set, which is consistent both with the low readings being real and with their being an artefact of an unconverged extraction. This pass could not separate the two. |
| Insertion loss comes back **positive** on most pairs | **Gain, which is impossible, and it is the same truncation noise as \|Sdd21\| > 1.** 11 of the 22 insertion-loss figures in the batch are above zero, by up to 0.78 dB. The `insertion_loss` verdict still reads `pass` everywhere, because its threshold is “not worse than −3 dB” — on this data that verdict means *no evidence of excess loss*, not a measured loss. Not fixed; the fix is a converged run, not a different threshold. |
| Worst-in-band \|Scd21\| is not comparable across net classes | **A limitation of the reported scalar, found while answering the skew question and deliberately left alone.** `mode_conversion_db` is the maximum over each class's *own* band, so a `pcie` pair swept to 8 GHz and a `sata` pair swept to 6 GHz are not compared over the same interval; and on four of the eleven that maximum lands between 0.50 and 0.61 GHz — the bottom of the band, where the extraction is noisiest and skew-driven conversion is smallest — including two of the three `warn`s. Recorded rather than changed, because changing it changes a metric this milestone publishes. |
| Simulated impedance sits systematically **below** every class target | **Not a defect in the router and not fixed.** Same cause as the first row: every bundled example pours ground up to its pairs at the class clearance, and neither closed form aipcb derives widths from knows about coplanar ground. The widths the tool derives are therefore systematically narrow for the boards it actually builds. Recorded in `docs/roadmap.md`; fixing it means a CPWG model and regenerating every controlled-impedance example. |
| "worst-case deviation **and its location along the trace** where derivable" | **The deviation is reported; the location is not.** Locating an impedance discontinuity along a trace needs a time-domain reflectometry transform of the reflection data, which is a second piece of numerics with its own convergence questions. What is reported instead is the median, the minimum and the maximum of the impedance across the swept band, which bounds the excursion without claiming to place it. Deliberately not built. |
| `--parallel N` | **Not built, and measured rather than assumed.** openEMS already uses every core — it sustained 300–1270 MCells/s on sixteen here — so a second process divides the same cores. ADR 0011 Decision 4. |
| scikit-rf | **Not adopted.** The post-processing is forty lines of `cmath`; adding a numeric stack to the install path of a YAML-and-pydantic tool for that is a bad trade. Every run writes a Touchstone `.s4p` so anyone who wants scikit-rf has the data. |
| `aipcb export` now writes **two** drill files where it wrote one | **A change to what a fab receives**, made deliberately: gerber2ems globs for `*-PTH.drl` and `sys.exit(1)`s when the glob is empty, and splitting by plating is what KiCad's own dialog does by default. ADR 0011, "the drill file had a name nothing globs for". |
| The `.kicad_pcb` stackup disagrees with the source stackup | **Found by this milestone and deliberately *not* fixed.** `compile/board.py` writes KiCad three equal 0.48 mm dielectrics for `examples/pcie-sata`, where the source declares 0.2104 / 1.065 / 0.2104 mm — and impedance is derived from the source. Fixing it changes every existing board, which M12's guardrails forbid. Simulation uses the source stackup and says so. ADR 0011 Decision 2. |
| `epsilon_r` per dielectric, and core-versus-prepreg typing | **Still unreachable**, for the same byte-stability reason. `loss_tangent` was made reachable because it costs nothing (no example sets it, so no board moves) and because it is the single number that decides insertion loss. |
| Energy decay | **The runs do not converge in time the way the vendor's examples do.** gerber2ems's own `stub_short` decays to −70 dB; the `pcie-sata` slices bottom out anywhere between −72 dB and −31 dB — the figure the tool records per pair, tabled under “What the batch cost” — because a slice with two plane layers and an artificial boundary is a resonant cavity, and the big ones are the worse cavities. The consequence is a few percent of truncation noise, visible as \|Sdd21\| a little above unity. It is characterised below rather than hidden, and it is why `max_steps` is a bounded default. |
| Crosstalk, eye diagrams, IBIS, thermal, PDN | Out of scope by the milestone's own text; recorded in `docs/roadmap.md`. |

Nothing else was descoped.

## What was built

| Piece | Where it lives | Lines |
|---|---|---|
| M12a — which pairs there are, merged across AC-coupling parts | `si/pairs.py` | 181 |
| M12a — cutting one pair out of a routed board, ports and all | `si/slice.py` | 698 |
| M12a — the three files gerber2ems reads besides the Gerbers | `si/inputs.py` | 216 |
| M12b — the container, the cache, and the silent-failure checks | `si/runner.py` | 262 |
| M12b — the batch | `si/run.py` | 379 |
| M12c — S-parameters to verdicts, and Touchstone out | `si/results.py` | 414 |
| the command | `cli_simulate.py` | 226 |
| the `simulation:` source block | `model/simulation.py` | 136 |
| build-and-route, lifted out of `route all` so two commands can use it | `route/pipeline.py` | 108 |
| tests | `tests/test_simulation.py` | 608 |

Two existing files changed in ways a fab can see: `compile/export.py` (two flags) and
`compile/board.py` (`loss_tangent` reaches the KiCad stackup when the source sets
one). `model/layout.py` gained `StackupLayer.loss_tangent` and a named
`DEFAULT_LOSS_TANGENT`.

`route/pipeline.py` is a *move*, not a rewrite: `aipcb route all` now calls it and
its output is byte-identical to before, verified on `examples/mcu-4layer` and
`examples/diff-pair` before anything else was changed.

## How a pair becomes a simulation

```
routed .kicad_pcb
   │
   ├─ logical_pairs()      12 declared pairs on pcie-sata → 11 physical links
   │                       (the transmit lane's two pairs and two capacitors are one link)
   │
   ├─ build_slice()        the pair's copper + everything near it, clipped to a
   │                       tight outline; the capacitors bridged; a straight launch
   │                       grown at each end; four Simulation_Port footprints
   │
   ├─ export_board()       kicad-cli plots it like any other board — ADR 0001 holds,
   │                       aipcb still writes no Gerber itself
   │
   ├─ write_inputs()       stackup.json from the *source* stackup, simulation.json,
   │                       netinfo.json
   │
   ├─ nets_in_gerbers()    read the export back before spending a minute on it
   │
   ├─ slice_digest()       sha256(slice.kicad_pcb + simulation.json + stackup.json)
   │                       → skip if unchanged
   │
   ├─ podman run           gerber2ems -a, geometry + two excitations + postprocess
   │
   └─ analyse()            mixed-mode extraction → Zdiff, RL, IL, verdicts, Touchstone
```

Four things in the slice are not on the real board, and each is an approximation
with a reason:

* **The coupling capacitors become copper.** At 4 GHz a 100 nF capacitor is a
  milliohm; the part under test is the trace on both sides of it. aipcb knows which
  parts those are because the source says `role: ac_coupling` — this is the one place
  where a source-level slicer can do something a geometric one cannot, and on
  `examples/pcie-sata` it is the difference between one 18 mm link and two 10 mm
  stubs with a port in the middle of a capacitor.
* **Each end grows a 1.5 mm straight launch.** openEMS builds a microstrip port
  along a grid axis; a trace that ends on a diagonal at a pad has nowhere to be fed
  from. The port occupies exactly the launch, so it adds no trace — but the pad and
  its antipad are replaced by a uniform line, and the discontinuity that was really
  there is not modelled.
* **The corridor the launches occupy is cleared** of other nets' copper. Past the
  pad is the component, and with the footprints gone that region holds the next
  pins' fanout — on `examples/pcie-sata`, a ground track within a millimetre of every
  pair end. See failure 5 below.
* **Footprints are dropped.** The interior of a run, which is what an impedance
  target is about, is untouched. The last few tenths of a millimetre at each end are
  not modelled.

## Five silent failures, and what caught each one

Phase 0's most useful conclusion was that this integration's failure mode is a
confident wrong answer at a zero exit code, and that whatever aipcb built must assert
the solver saw its inputs rather than trusting the exit status. That turned out to be
exactly right — and to be an understatement. Two of the five below were in the export
(phase 0 predicted them), one was in KiCad's behaviour on a board with no footprints,
and **two were in the slicer this milestone wrote**. Every one of them produced a
board that looked correct, a solver that exited zero, and a number that was wrong.

### 1. The placement file was in the wrong frame (found by reading the file)

Phase 0's fix gave the file a name the consumer globs for. It did not give it the
right *coordinates*: `pcb export pos` defaults to absolute page coordinates, so every
row read like

```
"C1","C_100N_0603","C_0603_1608Metric",145.500000,-122.500000,0.000000,top
```

— the board's position on an A4 sheet, with KiCad's downward Y. Adding
`--use-drill-file-origin` turns that into `45.500000,21.500000`: the part's position
on the board. Verified by reading the exported rows back against the board's own
footprint positions, because the *drill* half of this bug survived a flag that was
already correct, and a test that checks flags would have missed both.

### 2. The drill file had a name nothing globs for

`<board>.drl`, one `MixedPlating` file. gerber2ems globs `*-PTH.drl` and exits 1.
Now `--excellon-separate-th`, which is KiCad's own default in the GUI. This changes
the fabrication package for every design and is flagged as a deviation above.

### 3. KiCad prunes padless nets by renumbering — the one nobody predicted

A slice has tracks and, before this fix, no footprints at all. `pcbnew` drops every
net that no pad references when it loads a board, and it drops them by
**renumbering**: the tracks keep their stored codes, which now point at other nets.
On the first `mcu-4layer` slice that actually ran end to end, the pair's copper came
back labelled `DEV_DM` / `DEV_DP` — its neighbours.

Measured, on that run:

| | |
|---|---|
| Segments in the slice board | 18 `USB_DM`, 17 `USB_DP`, 2 `DEV_DM`, 2 `DEV_DP` |
| Draws in the exported `F_Cu.gbr` under `%TO.N,DEV_DM*%` | **40** |
| Net names the Gerber carried | `DEV_DM`, `DEV_DP`, `GND`, `VBUS` — no `USB_*` |
| What gerber2ems then refined the mesh around | nothing |
| Resulting grid on a 12.5 × 17.5 mm slice | 109 × 109 × 62, **114 µm** average cell |
| What that does to a 0.25 mm trace with 0.2 mm of clearance | merges it into the ground pour it sits in |
| What came back after 4 min 16 s of solving | S₀₀ = −1.005 (a short), \|S₁₀\| = 0.0007 |
| Exit code | **0** |

The fix is two-part and both parts matter. Each port footprint carries one 50 µm pad
on the launch it sits on — copper entirely inside a 200–250 µm trace, so no geometry
moves, and the net survives the load. And **every run now reads its own Gerbers back**
before starting the container: if the pair's net attributes are not in the copper
Gerbers, the pair fails with a diagnostic instead of being solved for a minute and
answered wrongly.

After the fix, the same slice: `USB_DM` and `USB_DP` present in the Gerber, grid
105 × 152 × 28 at the *coarsest* setting tried, \|S₁₀\| = 0.95.

### 4. The two launches of one pair ran on the same line — ours

The launch direction was the average of the two traces' outgoing directions. That is
right for a pair that runs *along* the board and wrong for a pair that leaves its pads
**broadside** — and `examples/pcie-sata`'s three PCIe lanes leave the controller
exactly like that: 0.5 mm apart in x, both heading down the board. The average comes
out along x, both launches run on one line, they overlap, and P is welded to N.

Measured on the transmit lane before the fix, with a completely plausible-looking
board:

| | before | after |
|---|---|---|
| peak \|Sdd21\| | **7.26** | 1.14 |
| worst return loss | **+40.7 dB** | −8.3 dB |
| differential impedance | **436.6 Ω** against 85 Ω | 66.6 Ω |
| energy decay | −35 dB, plateaued | **−70 dB** |
| exit code | 0 | 0 |

The rule is now perpendicular to the pair's own separation, with only the *sign* taken
from where the conductor heads. Five of pcie-sata's twenty-two pair ends were
collinear before it; a test now asserts the property on every pair on the board.

The verdict machinery did its job here — the run came back `impedance=unusable` on the
\|Sdd21\| > 1.15 rule, so nothing wrong was ever reported as a result. But it was one
threshold away from being reported as an impedance, and finding it took reading the
copper rather than reading the numbers.

### 5. The launch ran out past the pad, into the next pin's fanout — also ours

Subtler, with no wrong-looking geometry at all. A launch runs *outward* from where the
pair ends. Where a pair ends is a pad, and past the pad is the component. A slice drops
the footprints, so that region is not empty — it is the neighbouring pins' escape
routing. Measured on `examples/pcie-sata`: **every one of the twenty-two pair ends has
a ground track crossing within a millimetre of it.** A 1.5 mm launch laid straight
through is shorted to ground, and the first working batch was doing that on all eleven
pairs.

Shortening the launch until it fits was tried first and measured badly: on this board
there is frequently no collision-free length at all, so every port collapsed to a
0.4 mm floor — two trace widths, too short to de-embed. What ships instead **clears the
corridor**: foreign tracks are cut where they cross it, vias inside it are dropped, and
the slice reports how much it removed.

| Pair | foreign track removed | vias dropped |
|---|---|---|
| `PCIE_TXN+PCIE_TXP` | 5.02 mm | 0 |
| `SATA2_RXN+SATA2_RXP` | 4.02 mm | 0 |
| `SATA2_TXN+SATA2_TXP` | 3.90 mm | 1 |
| `REFCLKN+REFCLKP` | 3.61 mm | 1 |
| `PCIE_RXN+PCIE_RXP` | 2.48 mm | 0 |
| the other six | 1.00–2.32 mm | 0–1 |

That is a deliberate modification of the same kind as dropping the footprints and
bridging the coupling capacitors: **the port stands in for the pad and for everything
past it** — the driver, the connector, the cable — and the neighbouring pins' fanout is
not the transmission line under test. What matters is that it is visible: it is in the
slice's own `notes`, in `--json`, and in the console output.

## The source block, and what it does not contain

```yaml
simulation:
  classes:
    sata:    { stop_ghz: 6.0 }     # SATA III: 3 GHz fundamental
    pcie_tx: { stop_ghz: 8.0 }     # PCIe Gen3: 4 GHz fundamental
    pcie_rx: { stop_ghz: 8.0 }
```

That is the whole of what `examples/pcie-sata` had to say. Everything else —
which nets are a pair, which capacitors are shorts, what the laminate is, what the
impedance target is, which plane each pair is referenced to, how wide the trace is —
was already in the design, because M8 through M11 put it there. Mesh density, slice
margin and the pass thresholds are settable and are left at their defaults.

`impedance_tolerance`, `return_loss_db` and `insertion_loss_db` default to ±10 %,
−10 dB and −3 dB. **These are engineering defaults, not standards compliance.** Ten
percent and −10 dB are what most controlled-impedance fabrication notes ask for;
nothing here certifies a design against PCIe or SATA, and a link with a real budget
should state its own numbers.

Adding the block to `examples/pcie-sata` left its `.kicad_pcb`, `.kicad_sch` and
`.kicad_pro` byte-identical to the golden files — verified before anything else.

## What a run leaves behind

`out/si/<pair>/` per pair:

| File | What it is |
|---|---|
| `slice.kicad_pcb` | the slice, openable in KiCad and byte-stable |
| `slice.json` | the ports, the outline, the conductor length, and any notes |
| `fab/` | the slice's Gerbers, drill and placement file, plus `stackup.json` |
| `simulation.json`, `netinfo.json` | what gerber2ems was told |
| `ems/` | the solver's own geometry, S-parameters and plots |
| `<pair>.s4p` | the raw S-matrix as Touchstone, for scikit-rf or anything else |
| `result.json` | the cache entry: the digest, the timing and the metrics |
| `run.log` | the full solver log, which a failure's hint points at |

and `manifest.json` beside them, recording every pair, its status, its digest, its
wall clock and the image digest the batch actually ran on.

The Touchstone file says what it does not know: only two of the four columns were
excited, because a differential pair needs both halves of one end driven and the
other two columns are the reciprocal of those. The header states it rather than
letting a reader assume a full four-port sweep.

## What this does not tell you

Stated here as plainly as M11e stated its own limit, because the number that comes
out of a field solver is more persuasive than the number that comes out of a rule and
therefore needs the caveat more, not less.

**The stackup simulated is the one the source declares.** Every impedance below is
computed from `layout.stackup` — the prepreg thickness, the permittivity, the
dissipation factor written in the design file. A fabricator who presses a different
prepreg builds a different impedance and nothing here will know. This validates the
*layout*: it finds a pair routed at the wrong width, a reference that is not where
the class said, a discontinuity a via transition introduced. It does not replace an
impedance coupon on the panel or a measurement on the finished board.

**The thresholds are engineering defaults.** ±10 % and −10 dB are conventional, not
normative. Nothing here is a compliance statement.

**The launch is not the pad.** Each port replaces the last 1.5 mm of the pair with a
uniform line of the same width. A real pad, its antipad in the plane and the fanout
neck into it are discontinuities that this deliberately does not model — so the
numbers describe the *run*, and a design whose problem is at the pad will look
cleaner here than it is.

**The energy does not always fully decay.** gerber2ems's own validated example settles
at −70 dB. Two of the `pcie-sata` slices do as well, at −70 and −72 dB; the rest
bottom out between −31 and −41 dB, and `examples/mcu-4layer`'s USB pair at
−27 dB, because a slice with plane layers and an artificial boundary is a small
resonant cavity. The residual shows up as a few
percent of noise, and as |Sdd21| a little above unity — above unity is physically
impossible and is a marker phase 0 used to call an extraction broken, so the
threshold that separates "truncation noise" from "broken" is *calibrated against
measured data*: phase 0's genuinely non-converging diff-pair slice peaks at 1.166,
these runs at 1.07–1.09, and the line sits at 1.15.

## Tests

`tests/test_simulation.py`, 608 lines, and **not one of them starts a container**.
The solver is external, pinned and slow; what is testable here is everything on this
side of it.

| What is tested | How |
|---|---|
| 12 declared pairs on `pcie-sata` become 11 links, with `C2`/`C3` named as the bridge | against the real design |
| every pair on `pcie-sata` slices, with four ports each | against the real routed board |
| a slice is byte-stable | slice twice, compare the dump |
| ports face inwards, land inside the outline, and carry the pair's nets | against the slice |
| a pair that changes layer is referenced to two planes and *says so* | `REFCLKP/N` |
| no copper reaches the slice outline | every segment, plus its half-width |
| every port carries a keeper pad, and the pad is smaller than the trace | the defect above |
| the pair's nets survive into the exported Gerbers | reads the `%TO.N` attributes back |
| the placement file puts the ports where the slice says, in the board's own frame | reads the CSV back against the slice |
| `*-PTH.drl` exists | the glob the consumer uses |
| the stackup follows the *source*, not the uniform default | 0.2104 / 1.065 / 0.2104 |
| `loss_tangent` is reachable from source and falls back to 0.02 | both paths |
| every length in `simulation.json` is micrometres | against the slice's millimetres |
| a matched line comes back at exactly its own impedance | analytic S-matrix |
| a 130 Ω line reads 130 Ω, not the 100 Ω of the ports | analytic S-matrix |
| \|Sdd21\| > 1 is reported as unusable, not as a number | analytic S-matrix |
| group delay matches a known 200 ps line | analytic S-matrix |
| the CSV reader survives gerber2ems's trailing commas | synthetic file |
| Touchstone is well-formed and says which columns are unknown | round trip |
| the cache digest moves when the source moves and not otherwise | three `--dry-run` runs |
| an unknown `--net` is an error, not an empty run | the CLI |
| unsimulated pairs are listed, not silently absent | `--json` |

## The reuse-versus-reimplement call

Phase 0 left this open, and the answer is **reimplement**. Antmicro's
`kicad-si-simulation-wrapper` targets KiCad 9 and generated the very examples phase 0
validated against, so it was a serious candidate. Four things decided against it, in
descending order of weight:

1. **A geometric slicer cannot know what a part is.** `examples/pcie-sata`'s transmit
   lane is four nets, two declared pairs and two capacitors, and it is *one*
   transmission line. The wrapper cuts a region out of a board; it has no way to know
   that `C2` and `C3` are shorts rather than components under test. aipcb does,
   because the source says `role: ac_coupling` — this is the milestone's own stated
   reason to prefer its own slicer, and on the flagship example it is the difference
   between one 18 mm link and two 10 mm stubs with a port inside a capacitor.
2. **It works through the `pcbnew` Python API, which aipcb deliberately keeps at
   arm's length.** ADR 0009 confines that import to a subprocess behind a version
   lock, for zone filling only. Adopting the wrapper would put it in the tool's own
   process.
3. **A slice has to be an aipcb board.** Same s-expression writer, same deterministic
   UUIDs, same stackup arithmetic — which is what makes "slice generation is
   byte-stable" a property rather than a hope.
4. **What was worth taking was the method, and the method is three sentences.** Crop
   to a region; put `Simulation_Port` footprints at the trace ends; hand the result to
   gerber2ems. That is reproduced here, and phase 0 had already reproduced it by hand.

The honest cost: port placement, launch construction and the mixed-mode extraction
were all ours to get right, and two of the three were wrong on the first attempt.
Reuse would not have avoided that — the wrapper's ports are placed by a human in
KiCad — but it belongs on the ledger.

## Determinism

| Property | Result |
|---|---|
| Slice generation | **Byte-stable.** Slicing the same routed board twice produces identical files; a test asserts it. UUIDs are hashes of the pair name and the element index, never of order. |
| Solver numerics | **Not bit-reproducible**, and openEMS makes no such claim: it is a threaded FDTD kernel whose reduction order depends on scheduling. |
| Verdicts | **Reproducible.** What the gate needs is that pass/warn does not flip between runs, and the mesh convergence study below bounds the run-to-run spread far below any threshold in use. |
| Every golden board | **Byte-identical** after every change in this milestone, including the `simulation:` block added to `examples/pcie-sata` — checked against all eleven examples' `.kicad_pcb`, `.kicad_sch` and `.kicad_pro`. |

## The calibration

Phase 0 set a condition on this milestone: *do not report an impedance number until a
known-good structure reproduces a known-good value.* It also said why the numbers on
`examples/diff-pair` moved by 3× between runs — a 0.25 mm trace 1.51 mm above its
nearest copper barely guides at all — and named `examples/mcu-4layer` as the board to
calibrate on instead: its In1.Cu GND plane sits 0.48 mm below F.Cu.

The pair is `USB_DM/USB_DP`, 0.25 mm wide at a 0.2 mm gap, on F.Cu over that plane,
with the board's GND pour 0.2 mm away on each side. Three mesh densities of the same
geometry:

| Mesh | grid | cells | wall clock | \|Sdd21\| peak | Zdiff | ripple min–max | group delay |
|---|---|---|---|---|---|---|---|
| coarse — 100 µm, 6 cells per dielectric | 105 × 152 × 28 | 0.45 M | **54 s** | 1.075 | **75.96 Ω** | 16–201 Ω | 134.7 ps |
| shipped default — 50 µm, 8 | 181 × 290 × 39 | 2.05 M | **390 s** | 1.056 | **74.19 Ω** | 25–156 Ω | 125.9 ps |
| fine — 25 µm, 16 | 247 × 331 × 62 | 5.07 M | **1371 s** | 1.065 | **78.12 Ω** | 31–156 Ω | 128.4 ps |

**The answer moves by 5.3 % across an 11-fold change in cell count and a 25-fold
change in run time.** That is the condition phase 0 attached to this milestone,
discharged: on `examples/diff-pair` the same exercise moved the answer by 3×. The
difference is the structure, exactly as phase 0 predicted — a plane 0.48 mm below the
trace guides, one 1.51 mm below it does not.

Two supporting checks that the ports are measuring the trace and not something else:

* **Group delay.** 20.7 mm of conductor came back at 126–135 ps, against the 126 ps
  that 6.1 ps/mm predicts for FR-4 microstrip. Two undeembedded 1.5 mm launches would
  have added 18 ps, so openEMS is de-embedding them as intended.
* **Energy.** No run reports gain worth calling broken: peak \|Sdd21\| is 1.06–1.08
  where phase 0's non-converging slice reached 1.166.

50 µm and 8 cells per dielectric is what ships: it is within 5 % of the finest run
at a fifth of its cost, and the difference is well inside the ±10 % that a verdict
turns on.

### The sanity band, and why the design target is not it

The acceptance clause asks for a result "within a plausible sanity band of its design
target". The `usb` net class on `examples/mcu-4layer` declares no
`impedance_diff_ohm` at all — its target is implicit in its geometry — so the band has
to be built from what that geometry should give. aipcb's own two closed forms say:

| Model | Z₀ single-ended | Coupling factor | **Zdiff** |
|---|---|---|---|
| IPC-2141 (what the width solver uses) | 89.6 Ω | 0.678 | **121.5 Ω** |
| Hammerstad (what M8's gap estimator uses) | 93.3 Ω | 0.678 | **126.5 Ω** |
| **Simulated** | | | **74–78 Ω** |

That is 40 % low, and it is not noise: three mesh densities agree on it. The reason is
that **both formulas are bare-microstrip models and this is not a bare microstrip.**
`examples/mcu-4layer` pours GND on F.Cu, and the pour comes to within the class's
0.2 mm clearance of each trace. A pair with ground 0.48 mm below it *and* 0.2 mm
either side of it is a coplanar waveguide with ground, whose impedance is
substantially lower than the microstrip the formula solved for.

So the band chosen is: **the simulated impedance must lie between the coplanar limit
and the microstrip limit, and nearer the coplanar one.** It does, and the sign is
right — coplanar ground can only pull the impedance *down*, never up. A simulated
result *above* 121 Ω would have meant the model was wrong; a result at 74 Ω means the
formula is answering a question about a different board.

That is a finding about `impedance.py`, not about the router, and it is the most
useful thing this milestone measured. It also predicts what the flagship board will
do: `examples/pcie-sata` pours ground the same way, so every pair on it should come
back below its declared target by a similar fraction. It does — see the next section.

## The batch: the flagship board, pair by pair

This is the milestone's central run. The board is M11's routed `examples/pcie-sata`;
its twelve declared pairs merge into **eleven logical links** (`PCIE_TXP/N` and
`PCIE_TXP_C/N_C` are one link, bridged by `C2`/`C3`), and `aipcb simulate` slices,
solves and analyses each one. Every number in the three tables below was read out of
the tool's own `manifest.json` — none of it was recomputed by hand — on image
`gerber2ems:phase0`, digest `sha256:e1921ec2b3fc…`, under podman.

Ports are terminated at **half the class's declared differential target** — 42.5 Ω a
port for the 85 Ω PCIe classes, 50 Ω for the 100 Ω SATA class — so what the impedance
is read from is the line's own reflection rather than the termination's.

### Impedance against the declared target

| Pair | Class | Layer(s) | Declared | **Simulated Zdiff** | Deviation | Ripple min–max | Verdict |
|---|---|---|---|---|---|---|---|
| `PCIE_TXN+PCIE_TXP` | `pcie_tx` | F.Cu | 85 Ω | **66.6 Ω** | **−21.6 %** | 41–87 Ω | warn |
| `PCIE_RXN+PCIE_RXP` | `pcie_rx` | B.Cu+F.Cu | 85 Ω | **61.5 Ω** | **−27.6 %** | 46–134 Ω | warn |
| `REFCLKN+REFCLKP` | `pcie_rx` | B.Cu+F.Cu | 85 Ω | **50.9 Ω** | **−40.1 %** | 28–71 Ω | warn |
| `SATA0_TXN+SATA0_TXP` | `sata` | F.Cu | 100 Ω | **85.1 Ω** | **−14.9 %** | 77–99 Ω | warn |
| `SATA0_RXN+SATA0_RXP` | `sata` | F.Cu | 100 Ω | **91.9 Ω** | **−8.1 %** | 80–105 Ω | pass |
| `SATA1_TXN+SATA1_TXP` | `sata` | F.Cu | 100 Ω | **90.0 Ω** | **−10.0 %** | 79–104 Ω | pass |
| `SATA1_RXN+SATA1_RXP` | `sata` | F.Cu | 100 Ω | **81.0 Ω** | **−19.0 %** | 72–93 Ω | warn |
| `SATA2_TXN+SATA2_TXP` | `sata` | F.Cu | 100 Ω | **76.4 Ω** | **−23.6 %** | 67–87 Ω | warn |
| `SATA2_RXN+SATA2_RXP` | `sata` | F.Cu | 100 Ω | **80.0 Ω** | **−20.0 %** | 70–89 Ω | warn |
| `SATA3_TXN+SATA3_TXP` | `sata` | F.Cu | 100 Ω | **62.5 Ω** | **−37.5 %** | 37–107 Ω | warn |
| `SATA3_RXN+SATA3_RXP` | `sata` | F.Cu | 100 Ω | **67.7 Ω** | **−32.3 %** | 34–88 Ω | warn |

**All eleven links were simulated.** Nothing on this board is missing from the
table, and no pair on it was handed over unrouted by M11 for the table to
have to account for.

**Every one of the eleven came back below its declared target.** That confirms the
previous section's coplanar prediction in *sign*, on every pair. In magnitude it
confirms nothing: the deviations run from **−8.1 % to −40.1 %**, and "a similar
fraction" is not what that is. Two pairs pass the ±10 % default and nine warn.

**The most important number in this table is the spread inside a single net class.**
The eight `sata` links are as alike as this board makes two things: same class, same
derived width and gap, same layer for their whole run, coupled over 30.9–31.4 mm
every one of them, and M11 measured width and gap deviation at **0.0** on all eight.
They simulate at **62.5, 67.7, 76.4, 80.0, 81.0, 85.1, 90.0 and 91.9 Ω** — a **29 Ω
spread**, −37.5 % to −8.1 % against one 100 Ω target. Whatever a single pair's number
means, it carries an uncertainty of that order, and that is larger than most of the
deviations anyone would want to act on.

**Deviation, ripple width and return loss move together, almost rank for rank.** Sort
the eleven by deviation and the other two columns sort themselves:

| Group | Deviation | Ripple width | Worst return loss |
|---|---|---|---|
| `REFCLKP/N`, `SATA3_TXP/N`, `SATA3_RXP/N`, `PCIE_RXP/N` | −40.1 % to −27.6 % | 43–88 Ω | −4.6 to −8.3 dB |
| the other seven | −23.6 % to −8.1 % | 19–46 Ω | −8.3 to −18.4 dB |

Two readings fit that equally well and **this milestone cannot separate them**. Either
the low medians are real and the ringing is their consequence — a line far from the
impedance its ports are terminated at rings, and rings harder the further away it is —
or the ringing is an artefact of an extraction that has not converged and the median
is being dragged down by it. The first is the physically expected direction and the
one the coplanar-ground argument predicts. The second cannot be ruled out while the
energy floor sits where "What the batch cost" shows it sitting. What can be said is
that the four worst readings are also the four least trustworthy ones.

The two links that change layer through a via transition are `PCIE_RXP/N` and
`REFCLKP/N`, and the layer change is *not* what makes a deviation large: `SATA3_TXP/N`
reads −37.5 % without leaving F.Cu. What the transition does explain is those two
pairs' own `slice.json` note, which is worth repeating because it bounds how the row
should be read at all: *the pair changes layer, so its two ends are referenced to
different planes; the impedance reported is the whole link, not either half.* A
median taken across two cross-sections joined by a via barrel is not a characteristic
impedance.

### The rest of what each run says

| Pair | Band read | Worst RL | Insertion loss | Worst \|Scd21\| | Group delay | ps/mm | RL / IL / Scd21 |
|---|---|---|---|---|---|---|---|
| `PCIE_TXN+PCIE_TXP` | 0.50–8.00 GHz | −8.3 dB @ 7.78 GHz | 0.78 / 0.30 dB @ 4.0GHz / 8.0GHz | **−26.6 dB** @ 8.00 GHz | 124 ps | 6.07 | warn / pass / pass |
| `PCIE_RXN+PCIE_RXP` | 0.50–7.77 GHz | −7.7 dB @ 7.07 GHz | −0.40 / −1.18 dB @ 4.0GHz / 8.0GHz | **−25.2 dB** @ 7.71 GHz | 138 ps | 7.11 | warn / pass / pass |
| `REFCLKN+REFCLKP` | 0.50–7.76 GHz | −4.7 dB @ 7.27 GHz | 0.21 / −0.72 dB @ 4.0GHz / 8.0GHz | **−23.0 dB** @ 7.16 GHz | 138 ps | 7.05 | warn / pass / pass |
| `SATA0_TXN+SATA0_TXP` | 0.50–5.35 GHz | −16.9 dB @ 4.84 GHz | 0.41 / 0.40 dB @ 3.0GHz / 6.0GHz | **−25.7 dB** @ 0.50 GHz | 206 ps | 6.43 | pass / pass / pass |
| `SATA0_RXN+SATA0_RXP` | 0.50–5.45 GHz | −18.4 dB @ 4.83 GHz | 0.67 / 0.75 dB @ 3.0GHz / 6.0GHz | **−35.6 dB** @ 5.35 GHz | 202 ps | 6.39 | pass / pass / pass |
| `SATA1_TXN+SATA1_TXP` | 0.50–5.50 GHz | −18.1 dB @ 4.90 GHz | 0.35 / 0.30 dB @ 3.0GHz / 6.0GHz | **−29.0 dB** @ 6.00 GHz | 200 ps | 6.36 | pass / pass / pass |
| `SATA1_RXN+SATA1_RXP` | 0.50–5.43 GHz | −15.7 dB @ 4.91 GHz | 0.15 / 0.14 dB @ 3.0GHz / 6.0GHz | **−34.3 dB** @ 0.61 GHz | 203 ps | 6.38 | pass / pass / pass |
| `SATA2_TXN+SATA2_TXP` | 0.50–5.28 GHz | −14.1 dB @ 4.62 GHz | −0.19 / −0.16 dB @ 3.0GHz / 6.0GHz | **−19.4 dB** @ 0.50 GHz | 209 ps | 6.59 | pass / pass / warn |
| `SATA2_RXN+SATA2_RXP` | 0.50–5.43 GHz | −15.0 dB @ 4.72 GHz | −0.63 / −0.59 dB @ 3.0GHz / 6.0GHz | **−27.8 dB** @ 6.00 GHz | 203 ps | 6.47 | pass / pass / pass |
| `SATA3_TXN+SATA3_TXP` | 0.50–5.19 GHz | −5.4 dB @ 0.50 GHz | −1.83 / −1.32 dB @ 3.0GHz / 6.0GHz | **−15.7 dB** @ 0.53 GHz | 213 ps | 6.74 | warn / pass / warn |
| `SATA3_RXN+SATA3_RXP` | 0.50–5.04 GHz | −4.6 dB @ 0.78 GHz | −1.02 / −0.82 dB @ 3.0GHz / 6.0GHz | **−14.3 dB** @ 2.05 GHz | 220 ps | 6.86 | warn / pass / warn |

Four things in that table need saying rather than leaving to the reader.

**The return-loss column is not independent of the impedance column.** Each port is
referenced to the pair's *declared* target, so a line that comes back 28 % low must
reflect, and the return loss is largely that same mismatch expressed a second way. A
flat line at each pair's own median would give −15.9 dB for `PCIE_RXP/N` and
−27.5 dB for `SATA0_RXP/N`; the measured worst-in-band figures are −7.7 dB and
−18.4 dB. Measured is worse in both cases, because worst-in-band picks the peak of
the ripple rather than its centre. The PCIe `warn`s in this column and the PCIe `warn`s in
the impedance table are **one finding, not two**.

**11 of the 22 insertion-loss figures are positive, and positive insertion loss
is gain, which is impossible.** It is the same truncation noise that shows up as
|Sdd21| slightly above unity on every pair in this batch: the slice does not fully
decay, so a few percent of energy is still in the cavity when the transform is taken.
The `insertion_loss` verdict passes on every pair — but on this data that verdict
means *no evidence of excess loss*, not a measured loss, and it should be read that
way. The fix is a run that converges, not a different threshold.

**The group delay is the one column that cross-checks cleanly against M11.** The nine
links that stay on F.Cu measure **6.07–6.86 ps/mm**, against the ≈ 6.0 ps/mm M11
derived from first principles for this stackup (εr 4.4, h 0.2104 mm, w 0.2888 mm,
εeff ≈ 3.25) — agreement inside 15 %, from two completely independent routes, and
inside 8 % for six of the nine. The two links that change layer read 7.05 and
7.11 ps/mm; the denominator there is conductor length, which does not count the via
barrels the signal also travels through, so a higher apparent ps/mm is the expected
sign. All eleven sit inside the 3–8 ps/mm window `results.py` uses to check that the
ports are measuring the trace at all — which is the check that the whole batch is
looking at the traces and not at something else, and it passes everywhere.

**The mode-conversion column does not order by skew.** That is the answer to the
question M11 left open, and it gets its own section below.

### What the batch cost

| Pair | Status | Wall clock | Slice | Cells | Energy floor | Vias in slice |
|---|---|---|---|---|---|---|
| `PCIE_TXN+PCIE_TXP` | simulated | **0.9 min** | 4.7 × 25.4 mm | 0.56 M | −70.1 dB | 6 |
| `PCIE_RXN+PCIE_RXP` | simulated | **1.0 min** | 4.5 × 25.4 mm | 0.65 M | −71.8 dB | 8 |
| `REFCLKN+REFCLKP` | simulated | **2.2 min** | 5.4 × 25.4 mm | 0.91 M | −41.2 dB | 7 |
| `SATA0_TXN+SATA0_TXP` | simulated | **20.6 min** | 37.0 × 11.5 mm | 5.24 M | −31.0 dB | 8 |
| `SATA0_RXN+SATA0_RXP` | simulated | **14.7 min** | 37.0 × 9.3 mm | 4.11 M | −33.7 dB | 9 |
| `SATA1_TXN+SATA1_TXP` | simulated | **11.5 min** | 37.0 × 7.8 mm | 3.41 M | −34.4 dB | 0 |
| `SATA1_RXN+SATA1_RXP` | simulated | **18.1 min** | 37.0 × 10.0 mm | 4.69 M | −31.2 dB | 3 |
| `SATA2_TXN+SATA2_TXP` | simulated | **16.3 min** | 37.0 × 10.0 mm | 4.43 M | −27.6 dB | 8 |
| `SATA2_RXN+SATA2_RXP` | simulated | **10.9 min** | 37.0 × 7.7 mm | 3.30 M | −32.0 dB | 2 |
| `SATA3_TXN+SATA3_TXP` | simulated | **14.7 min** | 37.0 × 9.2 mm | 4.07 M | −39.9 dB | 9 |
| `SATA3_RXN+SATA3_RXP` | simulated | **19.7 min** | 37.0 × 11.5 mm | 5.21 M | −39.9 dB | 9 |

**The eleven together cost 7 837 s of solver time — 2 h 11 min.** A re-run with
nothing changed costs **0.3 s**, because every pair hits the digest cache and the
analysis is redone from the stored S-parameters rather than the solver. That gap is
the whole argument for the cache, and it is what makes this runnable in anything
resembling CI.

The wall clocks were taken across two sessions on the same sixteen-core machine, and
it was not idle throughout the second one. Read them as the right order of magnitude
per pair rather than as a benchmark; the ratios between slice sizes are the durable
part, not the absolute seconds.

What those numbers mean for CI — why cost tracks area, why the spread is what it is,
and why there is no `--parallel` — is worked through under "Performance" below. The
one thing to read straight off the table here is the **energy floor**, because it is
where the truncation noise in the two columns above comes from: `PCIE_TXP/N` and
`PCIE_RXP/N` decayed to −70 dB and −72 dB and stopped early, and nothing else did:
`REFCLKP/N` got to −41 dB and the eight SATA slices bottom out between −27.6 dB and
−39.9 dB, all of them running their step limit out.

## Open questions

* **The impedance formulas do not know about coplanar ground, and every board here
  has it.** This is the biggest single finding and it is not a simulation problem: it
  is that `impedance.py` derives a width from a bare-microstrip model, and then the
  pour comes within a class clearance of the trace and moves the answer. The
  simulated numbers are consistently below target by a similar fraction on every
  pair, which is the signature of a model error rather than a routing error. Fixing
  it means a coplanar-waveguide-with-ground model and regenerating every
  controlled-impedance example, so it is recorded rather than done.
* **Should the pour keep further away from a controlled-impedance pair?** The other
  side of the same coin, and a design question rather than a code one. A pour at
  3 × the class clearance would make the microstrip model true again; a pour at 1 ×
  gives better shielding and a lower impedance than anyone asked for. Nothing in
  aipcb has an opinion today.
* **The slice's boundary is a resonator.** Energy plateaus around −20 to −27 dB
  instead of decaying to −50, which costs a few percent of accuracy. A perfectly
  matched layer boundary, or lossier plane material, would help; gerber2ems calls
  `set_boundary_conditions(pml=False)` and does not expose the choice.
* **A killed client leaves its container running.** `run_gerber2ems` names the
  container so that a *timeout* can `podman rm -f` it, and that path works — but it
  is the only path. When the client process is killed outright, nothing runs, and the
  solver carries on to completion unattended. That is not hypothetical: the session
  that produced the batch above was killed mid-pair, and its container was still
  running sixteen cores' worth of FDTD eleven minutes later, on the same work
  directory a later run then started writing into. Two writers on one `ems/` is a way
  to get results that belong to neither. A pre-flight check for a live
  `aipcb-si-*` container on the same directory would cost nothing; it is recorded
  rather than built, because M12's guardrails put `si/runner.py` out of scope for
  this pass.
* **A digest-pinned Containerfile.** Phase 0 already recorded that upstream pins its
  base image by tag and installs from moving apt archives, so `aipcb simulate` is
  pinned by *image* and not reproducibly rebuildable. Unchanged.
* **`aipcb export` still ships an unrouted board.** Phase 0's gap 10. `aipcb
  simulate` works around it by routing for itself; the surprising default is still
  there.
* **Crosstalk is present but not measured.** A slice carries its neighbours' copper,
  so their loading is in the answer — but nothing excites them and nothing reports
  pair-to-pair coupling. That is the obvious next stage and is in
  `docs/roadmap.md`.

## The two pairs M11 left open

M11's own report flagged one thing as its closest call: `REFCLKP/N` and `PCIE_RXP/N`
are *delivered* at **0.292 mm** and **0.256 mm** of intra-pair skew against the
**0.125 mm** their class declares — routed and annotated rather than handed over. The
milestone owner's question for M12 was whether simulation discriminates them.

**It does not — and on the reported verdict it does worse than not discriminating.**
Of the eleven links, three come back `mode_conversion=warn`. None of the three is one
of these two. All three are pairs M11 routed comfortably *inside* their skew budget,
and both pairs that miss their budget pass. The verdict is not merely uninformative
about skew on this board; it is pointing the other way.

### Why Scd21 is the right place to look

**Impedance cannot see skew.** A pair with a length mismatch has the same
cross-section as one without, and both read the same differential impedance. What
skew does is convert differential signal into common mode, and that is a quantity the
same four-port matrix already contains — Scd21, differential in, common out. M12c
reports it for that reason. So the question *is* answerable from data the batch
already produced, and this is where to answer it.

### What the pairs actually measure

Skew as M11 measured it, against worst in-band |Scd21| as M12c reports it:

| Pair | M11 intra-pair skew | Budget | As delay | Worst \|Scd21\| |
|---|---|---|---|---|
| `PCIE_TXN+PCIE_TXP` | **0.000 mm** | 0.125 mm | 0.00 ps | **−26.6 dB** |
| `SATA0_TXN+SATA0_TXP` | **0.004 mm** | 0.25 mm | 0.03 ps | **−25.7 dB** |
| `SATA3_RXN+SATA3_RXP` | **0.004 mm** | 0.25 mm | 0.03 ps | **−14.3 dB** |
| `SATA2_RXN+SATA2_RXP` | **0.021 mm** | 0.25 mm | 0.14 ps | **−27.8 dB** |
| `SATA3_TXN+SATA3_TXP` | **0.042 mm** | 0.25 mm | 0.28 ps | **−15.7 dB** |
| `SATA1_TXN+SATA1_TXP` | **0.049 mm** | 0.25 mm | 0.31 ps | **−29.0 dB** |
| `SATA2_TXN+SATA2_TXP` | **0.053 mm** | 0.25 mm | 0.35 ps | **−19.4 dB** |
| `SATA1_RXN+SATA1_RXP` | **0.124 mm** | 0.25 mm | 0.79 ps | **−34.3 dB** |
| `SATA0_RXN+SATA0_RXP` | **0.199 mm** | 0.25 mm | 1.27 ps | **−35.6 dB** |
| `PCIE_RXN+PCIE_RXP` | **0.256 mm** **over** | 0.125 mm | 1.82 ps | **−25.2 dB** |
| `REFCLKN+REFCLKP` | **0.292 mm** **over** | 0.125 mm | 2.06 ps | **−23.0 dB** |

**Ordered by skew, the reported number is not monotonic and is not close to it.** The
worst three figures on the board belong to `SATA3_RXP/N` (**−14.3 dB**, 0.004 mm of
skew), `SATA3_TXP/N` (**−15.7 dB**, 0.042 mm) and `SATA2_TXP/N` (**−19.4 dB**,
0.053 mm) — the *three* links, all far inside a 0.25 mm budget, that trip the −20 dB
default and come back `warn`. `REFCLKP/N` and `PCIE_RXP/N`, at 2.3× and 2.0× their own
budget, sit at −23.0 dB and −25.2 dB and **pass**. And `SATA0_RXP/N`, at 0.199 mm,
reads −35.6 dB: ten decibels *better* than `SATA0_TXP/N`, which has fifty times less
skew. Handed this column and asked to pick the two pairs that miss their budget, you
would pick the wrong ones.

Part of that is an artefact of taking a single worst-in-band scalar. Four of the
eleven worst points sit at 0.50–0.61 GHz — the very bottom of the analysed band,
where skew-driven conversion is at its *smallest* and the extraction is at its
noisiest — and two of those four are `warn`s. The PCIe classes also sweep to 8 GHz
while the SATA class sweeps to 6 GHz, so the column compares worst points taken over
different intervals. Neither problem is the whole story, though, because the ordering
stays scrambled at any single frequency too.

### The reason: a mode-conversion floor the skew sits underneath

Reading Scd21 across frequency instead of collapsing it to one number. Unlike the
tables above, these figures are **not** from `manifest.json` — the tool stores only
the worst-in-band scalar. They were computed from each pair's saved S-parameters by
calling `results.py`'s own `read_sparameters` and `_mixed_mode`, so the arithmetic is
the tool's even though the presentation is not:

| Frequency | `PCIE_TXP/N` 0.000 mm | `SATA0_TXP/N` 0.004 mm | `SATA3_RXP/N` 0.004 mm | `PCIE_RXP/N` **0.256 mm** | `REFCLKP/N` **0.292 mm** |
|---|---|---|---|---|---|
| 1 GHz | −29.5 dB | −28.3 dB | −23.5 dB | −28.3 dB | −40.9 dB |
| 2 GHz | −29.7 dB | −28.2 dB | −17.2 dB | −28.4 dB | −35.2 dB |
| 3 GHz | −29.1 dB | −29.5 dB | −25.3 dB | −27.3 dB | −33.1 dB |
| 4 GHz | −29.2 dB | −28.3 dB | −23.8 dB | −28.2 dB | −32.1 dB |
| 5 GHz | −29.9 dB | −28.3 dB | −27.1 dB | −30.0 dB | −27.3 dB |
| 6 GHz | −29.2 dB | *band ends* | *band ends* | −28.0 dB | −25.8 dB |
| 7 GHz | −27.6 dB | *band ends* | *band ends* | −26.1 dB | −23.7 dB |
| **skew-only model, 7 GHz** | *none* | −64.9 dB | −64.4 dB | −28.0 dB | −26.9 dB |

The last row is |sin(πfΔτ)| at 7 GHz for each link's *measured* Δτ — what the skew
alone would produce if nothing else on the board converted anything. The two SATA
columns are only swept to about 5.4 GHz; their model figures are shown at 7 GHz
regardless, to put 0.03 ps on the same scale as the rest.

**The three left-hand columns are links with no skew worth the name, and they are not
quiet.** `PCIE_TXP/N` and `SATA0_TXP/N` sit flat at −28 to −30 dB from 1 GHz to the
top of their bands. `SATA3_RXP/N` has no skew either — 0.004 mm, a skew-only model of
−64 dB — and it is not flat so much as *loud*, wandering between −17 and −27 dB
with a 6 dB excursion at 2 GHz that has nothing whatever to do with length matching.
All three levels are the **floor**: mode conversion produced by everything that is
not intra-pair length mismatch — the launch geometry, the via barrels, the ground
pour's asymmetry around each trace, and the truncation noise the whole extraction
carries. `SATA3_RXP/N` is the proof that the floor alone can trip the verdict, since
it does.

`REFCLKP/N` is the one link that visibly climbs, by **17 dB** across the band. That
climb is the skew signature and it matches the physics: an otherwise ideal pair with
skew Δτ converts as |sin(πfΔτ)|, which rises 6 dB per octave. REFCLK's measured
7.05 ps/mm over 0.2916 mm gives Δτ = **2.06 ps**, which predicts −43.8 dB at 1 GHz
rising to −26.9 dB at 7 GHz. The measurement tracks that to within **3.2 dB at every
point, and lies above it at every point but one** — which is exactly what a floor
added underneath a signal does.

`PCIE_RXP/N` does **not** climb. With 0.256 mm and Δτ = **1.82 ps** it is flat at −26
to −30 dB across its whole band, indistinguishable from the two zero-skew links. Its
own floor happens to sit about 12 dB higher than REFCLK's, and the conversion its
skew should produce never gets out from under it: 1.82 ps predicts −28.0 dB at the
very top of the band, which is where its floor already is.

### The verdict

**Simulation, as M12 measures and reports it, does not discriminate these two pairs.**

* The number the tool prints — worst |Scd21| in band — separates neither pair from a
  pair with zero skew, and orders the set close to arbitrarily. Its **verdict** is
  worse than that: the three `warn`s all land on links inside their budget, and both
  links outside their budget pass.
* Beneath it, one of the two (`REFCLKP/N`) does carry a clean, model-matching skew
  signature in its frequency response. The other (`PCIE_RXP/N`) does not, and no
  different reading of the same data will pull it out: its skew is 1.8 ps and its
  floor is at the level 1.8 ps would produce.
* **The floor is the dominant term and it is wildly uneven.** Across the seven links
  with essentially no skew (0.000–0.053 mm), the worst-in-band figure — which for
  those links *is* the floor — runs from **−29.0 dB to −14.3 dB**. Add `REFCLKP/N`,
  whose floor has to be below the −40.9 dB it reads at 1 GHz, and the floor varies by
  **more than 25 dB between pairs on one board**. The skew actually under test moves
  the number by about **3 dB**. A 3 dB signal cannot be read off a 25 dB baseline,
  and that is the whole answer in one line.

Two things follow, and they point in opposite directions, so both are stated.

**M11's physical argument survives.** M11 defended the delivery by estimating
0.292 mm at ≈ 1.8 ps, about 1.4 % of a Gen3 unit interval, and calling it something a
link budget absorbs. M12 measures the delay independently and gets **2.06 ps** — the
same number, from the pair's own transmission phase rather than from a closed form —
and finds the conversion it produces sitting at the level of the board's own
asymmetry. The claim that this skew is not what will break the link is corroborated.

**The acceptance question still gets a "no".** "Does simulation discriminate" is not
the same question as "is the skew harmful", and answering the second does not
discharge the first. A tool that cannot tell a 0.292 mm pair from a 0.000 mm pair —
and that flags three well-matched pairs while passing both mismatched ones — cannot
be used to sign off skew, and M12 should not be described as if it could. The
`mode_conversion` verdict is real and reproducible; what it is measuring on this
board is the floor, not the skew.

**The experiment that would settle it, and was not run:** route one pair twice on the
same board — once matched, once with a deliberately injected skew of a known several
picoseconds — and difference the two Scd21 curves. Differencing cancels the floor,
which is the thing in the way. That is a cheap run and it is the obvious next step;
it is named here rather than done, because it needs a board this milestone does not
have and the honest answer above does not depend on it.


## Performance, and what it means for CI

Phase 0 measured 30 s – 2 min a pair on a two-layer slice with no pour. M12's boards
are four-layer with a plane on each side of the signal and a ground pour around it,
and the range is wider: the per-pair numbers are in “What the batch cost” above.
What follows is what those numbers mean.

**A slice's cost is its area, not its length.** The three PCIe links are long and
narrow — 4.5–5.4 × 25.4 mm, 115–136 mm², 0.56–0.91 M cells — and cost 0.9 to
2.2 minutes. The eight SATA links are 7.7–11.5 × 37.0 mm, 286–425 mm², 3.30–5.24 M
cells, and cost 10.9 to 20.6 minutes. Cell count follows area at a fixed mesh density,
and among the eight SATA slices — none of which stops early — cost is close to linear
in cells: **3.3 to 3.9 minutes per million cells** across a 1.6× range of slice size.
The two PCIe slices that did decay early cost 1.6 min/M instead, which is what
stopping early buys.

**openEMS stops early when the energy decays, and that is most of the variance.** A
slice whose energy reaches −50 dB stops there; one that traps energy in a plane cavity
runs to `max_steps`. Two of the three PCIe slices decayed to −70 dB and −72 dB well
under the step limit; `REFCLKP/N` stopped at −41 dB and cost twice what those two
did, and `examples/mcu-4layer`'s USB pair never got past −27 dB and ran the limit
out every time. That is why `max_steps` is a bounded default rather than a large number.

**No `--parallel`, and the reason is in the same numbers.** openEMS sustained
300–1300 MCells/s on this machine's sixteen cores and scales with them. Two pairs at
once share those cores; the total does not move, and two containers each holding a
couple of gigabytes of field arrays is a way to run out of memory rather than a way to
finish sooner. If a second machine is available the batch is trivially splittable by
`--net-class`, which needs no code.

